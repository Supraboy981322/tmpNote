# the port that the HTTP server listens on
port : 8795

# name your server, users will see it on the web ui
name : //tmpNote

# the default web page when no page is specified
default_page : new

# maximum note size (stored in RAM, so make sure you have enough)
max_note_size : 10MB

# enable or disable escaping '&' in view page when escaping HTML for XSS
escape_html_ampersand : false

# log file (cleared when server starts)
log_file : log.json

# log level (valid options: 'debug', 'req', 'info', 'warn', 'err')
log_level : debug

# log format (valid options: 'txt', 'json')
log_format : json

# note compression (valid options: 'gzip', 'brotli')
compression : brotli
